/** Mirrors backend seat.hold.ttl-seconds (application.properties). */
export const HOLD_TTL_SECONDS = 120;

/** Mirrors backend SeatRequestLimits.MAX_SEATS_PER_REQUEST. */
export const MAX_SEATS_PER_HOLD = 8;
